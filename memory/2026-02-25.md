# 2026-02-25

## Agentes (ollama-agents) — Mission Control
- Se implementó un **Mission Control** mínimo para dar seguimiento a ejecuciones del pipeline `ollama-agents/pipeline/run.py`.
- Nuevo módulo: `ollama-agents/pipeline/mission_control.py`
  - Escribe artefactos por run en el repo objetivo: `<project>/.mission_control/<run_id>/summary.json` y `events.jsonl`.
  - Puntero: `<project>/.mission_control/latest` (archivo con el `run_id`).
  - Variable para desactivar: `PIPELINE_MISSION_CONTROL=0`.
- Se añadió CLI: `ollama-agents/pipeline/mc.py`
  - `list`, `show --run latest`, `tail --run latest`.
- Se instrumentó `ollama-agents/pipeline/run.py` para emitir eventos:
  - `run_started`, `safety_ok`, `agent_start/agent_stop` por rol, `gates` por iteración, `run_finished`.

## Totem (cajera/avatares)
- Pinotronic confirmó que el proyecto del tótem está en:
  - `/media/administrador/IA2/compartida/totem/`
  - contiene `Plan de Desarrollo Totem.txt`, `totem-avatar/`, `cajera-avatar-web/`.
- Próximo objetivo acordado: **Opción 2 = Paso 1.2 (Servicio STT)** con TDD y seguimiento por Mission Control.
- Bloqueo detectado: `totem-avatar` **no es repo git** (no hay `.git`).
  - El pipeline requiere git + working tree limpio para aplicar diffs y operar seguro.
  - Pasos sugeridos:
    - `git init` + `git add .` + `git commit -m "chore: baseline project structure for totem-avatar"`

## Nota operativa
- `memory_search` no estuvo disponible por cuota (429) del proveedor de embeddings; se trabajó verificando directamente en disco.
