# 2026-02-23

## Proyecto: Cajera Avatar Web (tótem)
- Ruta del proyecto: `/media/administrador/IA2/compartida/totem/cajera-avatar-web`
- Stack: FastAPI (mock endpoints) + React/Vite + Vitest.
- Frontend y backend se levantaron en LAN:
  - Frontend Vite: `http://192.168.0.158:5173/`
  - Backend FastAPI: `http://192.168.0.158:8000/health`
- Se integró avatar 3D Ready Player Me (realista) con URL:
  - `https://models.readyplayer.me/699d29be6e4b038c0e05e163.glb`
  - Implementación: `frontend/src/components/Avatar3D.tsx` con three + @react-three/fiber + drei.
  - Se añadió polyfill ResizeObserver para tests: `frontend/src/test/setup.ts`.
- Problema: Chrome bloquea STT/TTS (micrófono / Web Speech) cuando se abre por IP LAN; funciona mejor en `localhost` o con HTTPS.

## Flujo y decisiones de producto
- Cliente y cajera hablarán español (MVP).
- Imágenes de dispositivos (tarjeta/efectivo) serán fijas por ahora.
- Ticket: genérico por ahora.

## Tests / calidad
- Se añadió test Vitest que valida error 404 en `/api/account/resolve` mostrando "Cuenta no encontrada" en subtitle:
  - `frontend/src/App.test.tsx`
- Se mejoró parseo de errores en `frontend/src/lib/api.ts` para surfacing `{detail: "..."}`.
- En rama `agent-pipeline` se hicieron commits locales:
  - `4b25214` (detail errors + test 404)
  - `680c255` (Ready Player Me 3D + polyfill)

## Prioridad: optimización de agentes (Ollama pipeline)
- Se creó adapter para monorepo FastAPI+React en `ollama-agents/pipeline/adapters/fastapi_react_monorepo.py` y se registró en `__init__.py`.
- Se ajustó `ollama-agents/pipeline/run.py` con:
  - hints correctos de estructura (backend/src, frontend/src/App.tsx)
  - guardrails: bloquear paths inventados (ej. `frontend/tests/`, `frontend/src/components/App/`) y revertir con `git reset --hard` + `git clean -fd`.

## Próximo paso acordado
- Integrar un modelo de Ollama en el backend para que genere lo que dice la cajera:
  - Nuevo endpoint sugerido: `POST /api/assistant/respond`
  - Modelo debe responder idealmente JSON (acciones + texto) y backend ejecuta acciones (resolve/balance/payment/ticket) para evitar inventos.
  - Pendiente: definir modelo exacto (`ollama list`), idioma es-MX, y formato de respuesta JSON estricto.
