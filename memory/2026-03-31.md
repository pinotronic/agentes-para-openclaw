# 2026-03-31

## Sesión con Pinotronic

### Sprite Atlas Builder — Mejoras al TiledCutter

**Ubicación:** `/home/administrador/.openclaw/workspace/projects/sprite-atlas-builder/`

#### Mejoras implementadas hoy

1. **Pegar imagen desde clipboard (Ctrl+V)**
   - Handler en `TiledCutter.tsx`
   - Detecta `clipboardData.items` tipo imagen

2. **Pan y Zoom en el canvas**
   - `Space + drag` o botón central para mover
   - `Rueda del mouse` para zoom
   - Tecla `R` para reset vista
   - Estado: `offsetX`, `offsetY`, `scale`

3. **Fix de validación (basado en Red Team review)**
   - `computeGridConfig()` ahora valida: NaN, división por cero, negativos
   - Retorna `{cols: 0, rows: 0}` en vez de crashear
   - Separa lógica pura de mutación de estado

**Commits:**
- `50c5049 fix(TiledCutter): add input validation to computeGridConfig` ✅ push
- SSH key configurada correctamente para GitHub

### Red Team / Blue Team — Agents implementados

**Ubicación:** `~/.openclaw/workspace/ollama-agents/`

#### Nuevos agentes

| Agente | Rol | Propósito |
|--------|-----|-----------|
| `code_critic` | 🔴 Red Team | Critica código buscando fallos P0-P4 |
| `defender` | 🔵 Blue Team | Propone soluciones concretas |
| `coordinator` | 🎯 Orquestador | Coordina sub-agentes, maneja permisos, background tasks |

#### Pipeline
`~/.openclaw/workspace/ollama-agents/pipeline/run_redblue.py`

#### Prueba realizada
- Código: `updateGridCalculations` del TiledCutter
- Resultado: 15 críticas encontradas (3 P0, 4 P1, 4 P2, 2 P3, 2 P4), fix aplicado y validado

**Commits:**
- `91ad23b feat(agents): add coordinator agent for sub-agent orchestration` ✅ push
- `b048ff0 Merge: resolve conflicts with remote, keep upstream versions` ✅ push

### Revisión Red Team / Blue Team — CajaPort

**Ubicación código:** `/media/administrador/IA2/contenedores/proyectos/contenedores/backend/`

#### Problemas encontrados (35 críticas)

| Prioridad | Cantidad | Tipo |
|-----------|----------|------|
| P0 (Seguridad) | 10 | SQL Injection, Sin Auth, Upload malicioso, Info leak |
| P1 (Edge Cases) | 7 | Null checks, archivos vacíos, límites |
| P2 (Lógica) | 6 | Paginación, nulos sin manejo |
| P3 (Performance) | 5 | Carga completa DB, memory leaks |
| P4 (Arquitectura) | 5 | God Router, sin DI, hardcoded paths |

#### Críticos P0
- **SQL Injection** en `items.py` y `boxes.py` (`ilike(f"%{query}%")`)
- **Sin autenticación** — cualquier persona puede borrar todo
- **Upload sin validación** — puede subir archivos maliciosos
- **semantic_search** carga TODO el DB en memoria (P1 crítico)

#### Estado: Pendiente aplicar fixes
- ⚠️ `/media/administrador/IA2/` está montado como solo lectura
- Los fixes no se pudieron aplicar por permisos
- Necesario: cambiar permisos o copiar a otra ubicación

### Claude Code Source (leaked 2026-03-31)

**Ubicación:** `/media/administrador/IA2/compartida/claude-code/` (51MB)

**Análisis:**
- Código legítimo de Claude Code CLI
- Stack: TypeScript + Bun + React/Ink
- Sin malware encontrado
- Patrones interesantes para nuestros agentes

### Análisis de repositorios explorados

| Repo | Ubicación | Notes |
|------|-----------|-------|
| Side-Step | `/media/administrador/IA2/compartida/sherlock/` | Fine-tuning LoRA para audio |
| Deep-Live-Cam | `/media/administrador/IA2/compartida/Deep-Live-Cam/` | Face swapping (CPU only) |
| claude-code (leaked) | `/media/administrador/IA2/compartida/claude-code/` | Source analysis |

### Proyectos ya instalados en IA2

En `/media/administrador/IA2/contenedores/proyectos/`:
- `framepack_cu126_torch26` — Video generation
- `personaplex` — Voz conversacional
- `Z-Image` / `zimage2` — Generación de imágenes
- `mcp-app` — MCP relacionado

En Pinokio (según Pino): ComfyUI, RVC, Applio, Vibevoice Realtime

### Repo Agents — estado

- GitHub: `github.com/pinotronic/agentes-para-openclaw`
- SSH funcionando correctamente
- Commits al día

## Notas adicionales
- Pino se comunica por Telegram (texto + voz)
- Voz: MiniMax TTS Spanish_ConfidentWoman
- Canal webchat para comunicación principal
- SSH key ED25519 configurada en GitHub
