# 2026-04-02 — Evaluación de agentes y hardening

## ollama-agents — verificación objetiva post-run
- Se agregó validación post-run en `runner/ollama_agent.py` con `--verify-command`.
- Si el comando de verificación falla, el manifest ahora queda en `error` aunque el agente haya producido salida.
- `agents/manifest.py` fue ajustado para persistir output también en runs fallidos.
- Tests del runner/manifests verdes: `36 passed`.

## phaser-dkjr-eval — saneamiento del banco de pruebas
- El repo de evaluación quedó nuevamente en verde tras eliminar `src/game/State/GameState.ts`, archivo huérfano dejado por una corrida parcial del implementer.
- Se detectó una dependencia inválida en `package.json`: `@types/phaser` no existe en npm.
- Se eliminó esa dependencia y `npm install` volvió a funcionar.

## Implementer — nueva evidencia
- Falló un ticket mínimo por precondición del entorno (`vitest` faltante), no por lógica del ticket.
- Tras corregir el entorno, el implementer sí cerró un micro-ticket real:
  - creó `src/game/logic/clampLadderProgress.ts`
  - creó `src/game/logic/clampLadderProgress.test.ts`
  - validación externa manual: `npm run test` y `npm run build` ✅
- Conclusión provisional: sirve para tickets ultra-atómicos, pero sigue siendo poco fiable para tareas multiarchivo.

## Bug adicional del runner
- `latest/implementer.json` podía quedar apuntando a un run viejo aunque existiera uno más reciente.
- Se corrigió `get_agent_status(...)` para recuperar el manifest más nuevo y sanar automáticamente el puntero `latest`.

## Runner hardening adicional
- Se agregó `--require-path` en `runner/ollama_agent.py` para exigir artefactos esperados del ticket además de test/build.
- Resultado importante: ya no hay falsos `completed` cuando el agente no rompe el repo pero tampoco crea los archivos pedidos.
- Tests del runner actualizados y verdes (`31 passed`).

## Implementer — diagnóstico refinado
- El output más reciente mostró que el agente intenta razonar sobre conflictos internos en vez de ejecutar.
- Hallazgo concreto: intentó usar `write_file` con `file_path` en vez de `path` y asumió erróneamente que solo podía hacer una tool call total.
- Se reforzó `implementer.yaml` con:
  - invalidación explícita de instrucciones residuales tipo diff/Python backend para tickets frontend,
  - prohibición de pseudo-tools textuales,
  - esquemas exactos de argumentos para tools,
  - aclaración de múltiples tool calls por corrida.
- Aun así, siguió fallando en crear `evaluateLevelOutcome.ts` y `evaluateLevelOutcome.test.ts`.
- Conclusión: el siguiente cuello de botella probablemente ya es el modelo del implementer, no solo su prompt.

## Benchmark de modelos del implementer
- Comparación justa con el mismo prompt mejorado y el mismo ticket (`evaluateLevelOutcome`).
- `qwen3.5:9b`:
  - tiempo total aprox. `32.15s`
  - creó ambos archivos
  - tests y build verdes
- `qwen3-coder:30b`:
  - tiempo total aprox. `22.41s`
  - falló por `Agent exceeded maximum tool rounds (12)`
  - no creó los archivos requeridos
- Conclusión práctica: para este runner y este tipo de ticket, `qwen3.5:9b` es mejor porque completa la tarea verificada; el 30b solo fue más rápido en fallar.

## Perfil activo del implementer
- Se actualizó el perfil real usado por el runner en `ollama-agents/agents/implementer.yaml`.
- Ahora usa el prompt mejorado y mantiene `qwen3.5:9b` como modelo activo.
- Se añadieron reglas TypeScript/Vite específicas (`import type`, sin imports no usados, respetar número exacto de tests).
- Validación posterior exitosa:
  - archivos `evaluateLevelOutcome.*` presentes
  - `npm run test` verde
  - `npm run build` verde
  - status más reciente del implementer: `completed`

## Límite actual del implementer
- Con el perfil activo mejorado, el implementer todavía falla en un ticket multiarchivo de 4 archivos.
- Resultado observado:
  - sí creó `countRemainingCollectibles.ts`
  - no completó los otros 3 archivos requeridos
  - manifest en `error` por `Required path verification failed`
- Conclusión práctica: umbral actual estable = tickets pequeños; umbral inestable = paquetes multiarchivo.

## Test Writer
- Segunda iteración del `test_writer` con prompt reforzado: mejora insuficiente.
- Sigue mostrando `Thinking...`, inventando contratos y confundiendo semántica real de funciones.
- Benchmark de modelos:
  - `qwen3.5:9b`: salida defectuosa pero al menos útil para diagnosticar
  - `qwen3.5:27b`: peor, falló por `Agent exceeded maximum tool rounds (4)` sin salida final útil
- Conclusión práctica: para `test_writer` también conviene quedarse de momento con `qwen3.5:9b`.

## Modelos nuevos instalados y probados
- Instalados con éxito:
  - `devstral-small-2:latest`
  - `nemotron-cascade-2:latest`
- `qwen3-coder-next:latest` se instaló pero fue eliminado después por coste de disco/VRAM (51 GB, no razonable para 32 GB VRAM).

## Benchmark adicional de implementer
- `devstral-small-2:latest`:
  - comportamiento parcial útil
  - creó `isVictoryAchieved.ts` pero no `isVictoryAchieved.test.ts`
  - quedó en `error` por required paths faltantes
- `nemotron-cascade-2:latest`:
  - no usable en este runner para implementer
  - se quedó emitiendo `y` repetidamente hasta abortar la corrida
- Conclusión: `qwen3.5:9b` sigue siendo mejor implementer práctico en este entorno.

## Benchmark adicional de test_writer
- `devstral-small-2:latest`:
  - rápido, pero siguió inventando semántica y hasta usó bloques de código contra la instrucción
- `nemotron-cascade-2:latest`:
  - devolvió texto completo, pero siguió incluyendo `Thinking...`, fences y contratos inventados
- Conclusión: tampoco mejora al baseline; `qwen3.5:9b` sigue siendo el menos malo para `test_writer`.

## Estado final recomendado
- `implementer` restaurado a `qwen3.5:9b`
- `test_writer` restaurado a `qwen3.5:9b`
- Mantener `devstral-small-2` y `nemotron-cascade-2` instalados solo si quieres seguir explorándolos; por ahora no superan al baseline en este flujo.

## Diagnoser — promoción de baseline
- Benchmark directo sobre un error TypeScript real (`TS1484` + `TS6133` por import mixto de tipo/valor en `evaluateLevelOutcome.test.ts`).
- `llama3.2:3b` siguió inventando configuración/tooling y no se quedó en el fix mínimo local.
- `qwen3.5:9b` identificó correctamente que `LevelOutcome` debía importarse como `import type` y mantuvo mejor disciplina con la evidencia.
- Se promovió `ollama-agents/agents/diagnoser.yaml` a `model: qwen3.5:9b`.
- También se simplificó el prompt del diagnoser a una versión frontend-only y evidence-first; el prompt largo genérico estaba empujando alucinaciones.
- Se eliminó el override temporal `projects/phaser-dkjr-eval/.openclaw/agents/diagnoser.yaml` para evitar shadowing en benchmarks futuros.

## Diagnoser — benchmark de runtime del sistema
- Se probó el diagnoser promovido contra un fallo real del runtime del `implementer`, no del repo Phaser.
- Caso usado: `implementer` con `nemotron-cascade-2:latest` quedó en `running` y emitió `y` repetidas hasta aborto manual; además se dio como contraste un run de `devstral-small-2` con falso “éxito textual” pero `Required path verification failed` en manifest.
- Resultado: el diagnoser ya separa mejor problema de runtime/agente vs problema del proyecto, y sugirió guardrails razonables a alto nivel.
- Límite: todavía inventa detalles de implementación del runner y tests concretos no respaldados por lectura del código.
- Conclusión práctica: útil para triage inicial del sistema de agentes, pero no para aplicar fixes del runner sin revisión manual.

## Diagnoser — comparación con modelo pequeño alterno
- Se repitió el mismo caso de runtime del sistema usando un override temporal del diagnoser con `llama3.2:3b`.
- Resultado: `llama3.2:3b` volvió a dar un diagnóstico demasiado genérico y poco accionable.
- Conclusión: `qwen3.5:9b` sigue siendo claramente mejor baseline también para fallos de runtime del sistema, no solo para errores TypeScript locales.
- Se eliminó el override temporal tras el benchmark para no dejar shadowing en el repo de evaluación.

## Runner — guardrail streaming contra output degenerado
- Después del rechazo post-hoc de outputs degenerados, se endureció también `runner/ollama_agent.py` para inspeccionar stdout en streaming durante `ollama run`.
- Si la ventana reciente de salida se vuelve demasiado repetitiva (`y` u otro patrón dominante), el runner ahora mata el proceso y falla la corrida antes de aceptar texto final.
- Se añadieron tests unitarios del helper incremental y del flujo relacionado; validación específica: `8 passed` en `tests/test_runner.py -k 'degenerate or tool_support or stream_window'`.
- Sigue pendiente validar este guardrail con una corrida real del modelo problemático y revisar aparte la falla ajena de la suite completa de `test_runner.py`.