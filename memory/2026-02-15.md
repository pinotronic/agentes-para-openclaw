# 2026-02-15

- **X/Twitter Auth:** Pinotronic proveyó tokens manualmente (`auth_token`, `ct0`). No tengo la herramienta `bird` configurada en `available_skills` ni en `openclaw.json`. He guardado los tokens aquí por seguridad y he configurado `web_search` (Brave API) como fallback para el resumen diario.
- **Configuración:** Se aplicó `config.patch` para añadir la `BRAVE_API_KEY` recuperada de `MEMORY.md`.

## Agentes de desarrollo (ollama-agents)
- Pinotronic quiere mejorar los agentes (planner/test_writer/implementer/diagnoser/reviewer/debater) con **exámenes** por rol antes de trabajar en el proyecto `inmobiliaria`.
- Se eligió **Opción B**: benchmark alineado a stack **FastAPI + SQLModel + pytest**.
- Auth estándar para el benchmark: **JWT + refresh**.
- Se creó la batería de exámenes y rúbrica: `ollama-agents/eval/EXAMS_FASTAPI_SQLMODEL.md`.
- Se revisaron los YAML actuales de agentes: `ollama-agents/{planner,test_writer,implementer,diagnoser,reviewer,debater}.yaml` (ya asumen FastAPI/SQLModel).

## DIRECTOR.MD (source of truth)
- Pinotronic pidió que exista un solo `DIRECTOR.MD` canonical.
- Se confirmó que `director.md` (minúsculas) no existe; había copias idénticas de `DIRECTOR.MD`.
- Se eliminó todo duplicado y quedó **solo**: `/home/administrador/.openclaw/workspace/DIRECTOR.MD`.

## Proyecto benchmark: inmobiliaria-bench (FastAPI + SQLModel + pytest)
- Se completó el **Paso 1 prod-grade**: refresh tokens **persistidos en DB** (tabla `refresh_tokens`) + rotación + logout/revoke. Commit: `2600e87`.
- Se completó el **Paso 2**: hashing de passwords con **Argon2** (`argon2-cffi`), nuevas pruebas `tests/test_password_hashing.py`, y se actualizó el seed user `a@a.com` para usar Argon2. Commit: `77f46c8`.
- Se avanzó el **Paso 3 (puertos/repos)**:
  - Se crearon puertos `UserRepository`, `RefreshTokenRepository` y adapters SQLModel (`SqlModelUserRepo`, `SqlModelRefreshTokenRepo`). Commit: `0f7ed70`.
  - Se cerró el patrón para propiedades: `PropertyRepository` + `SqlModelPropertyRepo`, y `application/property_service.py` ya no depende de SQLModel como contrato. Commit: `6dbbb49`.
- Estado: `pytest -q` verde tras cada paso.

## Inmobiliaria-bench evoluciona hacia "programa inmobiliaria" (MVP)
### Bloques prod-grade completados
- **Paso 4 + 5**: routers limpios (`interfaces/http.py`, `interfaces/auth.py`, `interfaces/properties.py`), `main.py` solo wiring; Alembic baseline + migraciones. Commit: `fae3371`.
- **Init prod-like**: alembic en startup fuera de tests, DB file `dev.db` por defecto, y no drop/create fuera de tests. Commit: `1bd4e16`.
- **Seed dev opcional** (`SEED_DEV=1`) + `scripts/dev.sh`. Commit: `710f0e2`.
- **CI**: Ruff + GitHub Actions. Commit: `a3bb5a3`.
- **Índices**: refresh/properties indices + migración. Commit: `48be0f0`.
- **Purga refresh**:
  - purge expirados + test. Commit: `eb1679e`.
  - CLI purge expirados. Commit: `ec9fd19`.
  - purge-on-start con cooldown + estado `.state/refresh_purge.json`. Commit: `9a486b3`.
  - purge por `revoked_at` (retention) + scripts. Commit: `5f6a643` y consolidado en `b1c6afa`.
- **Observabilidad**:
  - trace_id middleware + error responses + tests. Commit: `d34dd3f`.
  - security headers + CORS opcional. Commit: `ebd4b92`.
  - logs de auth (`app.auth`) para login/refresh/logout/reuse/rate-limit. Commit: `85c4676`.
  - JSON logging opcional + fix trace_id en logs/captura pytest. Commits: `b1c6afa`, `ab174f6`.
- **Settings env-driven** (pydantic-settings): `DATABASE_URL`, JWT secret/TTLs, flags ops, rate-limit. Commit: `b0953aa`.
- **Hardening login**: rate limit in-memory en `/auth/login` + tests; reset state en `init_db(reset=True)`. Commits: `1e0d1c1`.
- **Postgres CI**: corre tests + alembic en Postgres via `DATABASE_URL` + `USE_DATABASE_URL_IN_TESTS=1`. Commit: `4d335d0`.

### Auth refresh prod-grade reforzado
- Refresh token ahora incluye:
  - `created_at`, `revoked_at` (+ migración). Commit: `5567dd1`.
  - `family_id` + reuse detection (revoca familia si se reusa token rotado) (+ migración). Commit: `1520073`.
  - logout revoca familia completa. Commit: `c483b26`.

### Requisitos MVP definidos por Pinotronic
- 2 áreas + login:
  - **Área pública**: home con cards (foto+ubicación+precio), detalle con todos los datos y mínimo 5 fotos.
  - **Área restringida**: listados de inmuebles; alta/edición; alta de agentes/supervisores/admin.
  - Login para agentes/supervisores/admin.
- Decisiones: moneda **MXN**; ubicación como **texto libre**.

### Público implementado en API
- `GET /public/properties` cards: `id, cover_url, location, price` solo `published=true` + `status=active`. Commit: `84f60ae`.
- `GET /public/properties/{id}` detalle público:
  - requiere published+active y **>=5 fotos**, si no → 404 `PROPERTY_NOT_FOUND`.
  - incluye features básicas (bedrooms/baños/estacionamiento/cocina/sala/salaTV) + fotos ordenadas (cover primero).
  - migración `b46140a2f830`.
  - Commit: `54882e0`.

### CMS: fotos (upload) implementado
- Se copiaron fixtures de fotos desde `/home/administrador/Descargas/*.jpeg` a `projects/inmobiliaria-bench/tests/fixtures/photos/`. Commit: `04f992d`.
- Endpoint nuevo: `POST /cms/properties/{id}/photos` (multipart) guarda archivo en `UPLOAD_DIR` (default `./uploads`) y crea `PropertyPhoto`. Roles: admin/agent/supervisor. Tests incluidos. Se añadió `python-multipart`. Commit: `db480c1`.

### Pendiente inmediato
- CMS: `GET /cms/properties`, `POST /cms/properties`, `PATCH /cms/properties/{id}`.
- Regla de publicación: solo permitir `published=true` si tiene >=5 fotos.
